# 프로젝트 핵심 원칙

이 프로젝트는 학습 및 기술 검증 프로젝트다.

## 핵심 불변식

이 프로젝트의 구현과 설계는 다음 불변식을 보장하는 것을 최우선 목표로 한다.

재고는 절대 0 미만이 되지 않는다.
하나의 주문에는 성공한 결제가 최대 하나만 존재한다.
외부 PG 요청의 timeout 또는 connection failure만으로 결제를 FAILED로 확정하지 않는다.
결제 결과를 확정할 수 없는 경우 UNKNOWN 상태를 사용한다.
최종 결제 상태가 오래된 webhook에 의해 이전 상태로 역행하지 않는다.
동일 webhook 또는 이벤트가 중복 전달되어도 비즈니스 효과가 중복 발생하지 않는다.
동일 환불 요청이 반복되어도 실제 환불 효과는 한 번만 발생한다.
애플리케이션 재시작 또는 인스턴스 장애 이후에도 PENDING/UNKNOWN 상태는 최종 상태로 수렴해야 한다.
로컬 주문/결제 상태와 PG의 authoritative state가 장기간 불일치 상태로 남아서는 안 된다.

각 Phase의 구현은 어떤 불변식을 보장하는지 명시해야 한다.

불변식을 애플리케이션 코드만으로 보장한다고 가정하지 말고, 가능한 경우 DB constraint, unique constraint, transaction boundary, 상태 전이 규칙 등을 함께 검토한다.

## 아키텍처 및 범위 원칙

프로젝트는 처음부터 MSA로 구성하지 않는다.

초기 구조는 하나의 애플리케이션과 하나의 관계형 데이터베이스를 사용하는 Modular Monolith를 기본으로 한다.

초기 모듈은 필요에 따라 다음과 같이 논리적으로 분리한다.

Order
Inventory
Payment
Refund
Reconciliation
Outbox
Admin/Operations

서비스 분리는 학습 목적이나 실제로 관찰된 문제를 해결하기 위해 필요한 경우에만 검토한다.

단순히 분산 시스템 기술을 사용하기 위해 다음 요소를 선행 도입하지 않는다.

독립적인 Order/Payment/Inventory 마이크로서비스
Kafka 등의 메시지 브로커
Redis distributed lock
Kubernetes
Saga framework
별도의 분산 트랜잭션 프레임워크

이러한 기술은 현재 단계에서 발생한 구체적인 문제와 대안을 먼저 분석한 후 도입 여부를 결정한다.

복수 인스턴스와 분산 환경에서의 correctness 검증은 프로젝트 후반부에서 동일 애플리케이션을 여러 인스턴스로 실행하는 방식으로 먼저 수행한다.

## 규칙:

1. 다음 단계 기능을 미리 구현하지 않는다.
2. 현재 단계에서 실패해야 하는 동작은 실제로 실패하도록 남긴다.
3. 실패를 테스트나 재현 스크립트로 고정한다.
4. 테스트를 실행하지 않았으면 통과했다고 표현하지 않는다.
5. 성능 수치를 실행하지 않고 추정하지 않는다.
6. DB constraint와 application validation을 구분한다.
7. 외부 API timeout을 결제 실패와 동일하게 처리하지 않는다.
8. 구현 후 다음을 보고한다. (docs/report 폴더에 파일 생성)
   - 변경 파일
   - 설계 이유
   - 실행한 명령
   - 테스트 결과
   - 아직 보장하지 않는 것
   - 다음 단계에서 해결할 문제
9. 구현 후 다음을 기록한다.
  - ADR은 중요한 결정과 그 이유를 기록한다.
  - Evidence는 실제로 실행한 명령과 결과를 기록한다.
  - Runbook은 장애 확인과 복구 절차를 기록한다.
10. 가능한 경우 경쟁 조건은 deterministic test 또는 반복 가능한
   reproduction script를 만든다.
11. 현재 단계의 acceptance criteria를 모두 충족하지 않으면
    다음 단계 구현을 시작하지 않는다.
