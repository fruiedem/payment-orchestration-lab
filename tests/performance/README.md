# Performance observations

Phase 1 does not claim a benchmark. The reproduction script records request latency and
exposes transaction timers through Actuator. Only values from an executed run belong in
`docs/evidence`; no throughput or production capacity is inferred from this lab.

향후 측정 가능한 부하 시나리오와 결과를 둔다. 현재 단계에서는 비어 있다.
